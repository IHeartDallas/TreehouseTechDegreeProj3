
// changes the text of h1 elements to "hello world"
$("h1").text("hello world");

// identify by id - change text
$("#main-heading").text("hello world");

// identify by class - change text
$(".note").text("hello world");

// identify by id - Get text - No arguments passed
//same function - but it works differently based on arguments passed
$("#main-heading").text();

var name = $('#name').val();
